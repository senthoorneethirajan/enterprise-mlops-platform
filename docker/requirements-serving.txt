# Serving-only dependencies, pinned EXACTLY to requirements.lock.txt (training env)
# so the pickled model deserializes identically. Regenerate when the lock changes.
numpy==2.5.1
pandas==2.3.3
scikit-learn==1.9.0
joblib==1.5.3
fastapi==0.140.0
uvicorn[standard]==0.51.0
prometheus-client==0.26.0
