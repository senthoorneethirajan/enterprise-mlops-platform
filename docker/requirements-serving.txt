# Serving-only dependencies. IMPORTANT: scikit-learn/joblib/numpy/pandas versions must
# match requirements.lock.txt (training env) so the pickled model loads identically.
# Sync these pins whenever the lock file changes.
numpy>=1.26,<3
pandas>=2.2,<3
scikit-learn>=1.4,<2
joblib>=1.3
fastapi>=0.110,<1
uvicorn[standard]>=0.29,<1
prometheus-client>=0.20
